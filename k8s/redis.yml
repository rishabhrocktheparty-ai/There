apiVersion: apps/v1
kind: Deployment
metadata:
  name: there-redis
  namespace: there-production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: there-redis
  template:
    metadata:
      labels:
        app: there-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
        volumeMounts:
        - name: redis-storage
          mountPath: /data
      volumes:
      - name: redis-storage
        persistentVolumeClaim:
          claimName: redis-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: there-redis
  namespace: there-production
spec:
  selector:
    app: there-redis
  ports:
  - port: 6379
    targetPort: 6379
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-pvc
  namespace: there-production
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi