# Frontend Environment Variables (Vite)
# All variables must be prefixed with VITE_ to be exposed to the client

# Environment
VITE_NODE_ENV=development

# Backend API Configuration
VITE_API_URL=http://localhost:3000
VITE_API_BASE_URL=/api
VITE_WS_URL=ws://localhost:3000

# Google OAuth Client ID (public, safe to expose)
# Get from: https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_CLIENT_ID=""

# Apple OAuth Client ID (public, safe to expose)
VITE_APPLE_CLIENT_ID=""

# GitHub OAuth Client ID (public, safe to expose)
VITE_GITHUB_CLIENT_ID=""

# Feature Flags
VITE_ENABLE_SOCIAL_LOGIN=true
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_DEBUG=true

# App Configuration
VITE_APP_NAME="There"
VITE_APP_VERSION="1.0.0"
VITE_APP_DESCRIPTION="AI Relationship Platform"

# Upload Configuration
VITE_MAX_FILE_SIZE=10485760
VITE_ALLOWED_FILE_TYPES="image/jpeg,image/png,image/gif,audio/mpeg,audio/wav"

# Session Configuration
VITE_SESSION_TIMEOUT=86400000
VITE_TOKEN_REFRESH_INTERVAL=3600000

# Note: Never put secrets (API keys, tokens, passwords) in frontend .env
# They will be exposed in the client-side JavaScript bundle!
